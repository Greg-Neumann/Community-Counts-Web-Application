@model IEnumerable<CommunityCounts.Models.Master.C1clientneedscat>

@{
    ViewBag.Title = "Needs";
     Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Client Needs Setup</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, htmlAttributes: new { @class = "btn btn-warning btn-xs" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.idNeedsCat }, htmlAttributes: new { @class = "btn btn-warning btn-xs" })
            @Html.ActionLink("Delete", "Delete", new { id=item.idNeedsCat },htmlAttributes: new { @class = "btn btn-danger btn-xs" })
        </td>
    </tr>
}

</table>
